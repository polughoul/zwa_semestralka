<?php
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $nname = $_POST['nname'];
    $password = $_POST['password'];

    // Выполняем проверку введенных данных на сервере.
    if (validateData($nname, $password)) {
        // Предположим, что аутентификация успешна.
        echo 'success';
    } else {
        // Вход неуспешен, отправляем сообщение об ошибке на клиентскую сторону.
        echo 'error';
    }
}

function validateData($nname, $password)
{
    // В этой функции нужно выполнить проверку введенных данных на сервере.
    // Примерно как в предыдущих ответах. Замените этот код на свою логику проверки.

    // В этом примере всегда возвращаем успешную аутентификацию.
    return true;
}
